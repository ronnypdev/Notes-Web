# Spec for auth auth-form-component

branch: notesweb-auth-implementation-v2

## Objective

To finish building the `AuthForm` component which will be use for the `/signup` `/login`, `/forgotpassword` and `/resetpassword` for this feature we're only focusing on the architecture of the AuthForm component no actual authentication or backend logic will be implemented at the moment this is purely a client side feature.

## Functional Requirements

- The file we'll be working with is in `/src/app/(auth)/components/AuthForm.tsx`
  - Since component it's already built for the most part we're going to use the code inside this file

* For the `/login` page the the `AuthForm` component most contain and handle the following specs:
  -
